{
    "name": "doncho/calculator",
    "type": "project",
    "description": "Fee calculator.",
    
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },

    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^6"
    }
}
